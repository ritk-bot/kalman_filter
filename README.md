# kalman_filter

To implement kalman filter on mpu6050 imu to be put on the rover's gripper.

The src folder contains the bevel package which you can paste into your own workspace, build it and run rviz simulation of the gripper along with the imu.

To use kalman filtering import the library in the libraries folder.
